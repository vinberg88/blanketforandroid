<resources>
    <!-- Base application theme for Blanket -->
    <style name="Theme.BlanketForAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_accent</item>
        <item name="colorPrimaryVariant">@color/blue_accent_dark</item>
        <item name="colorOnPrimary">@color/light_text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue_accent</item>
        <item name="colorSecondaryVariant">@color/blue_accent_dark</item>
        <item name="colorOnSecondary">@color/light_text</item>
        <!-- Background color. -->
        <item name="android:colorBackground">@color/dark_background</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="colorOnBackground">@color/light_text</item>
        <item name="colorOnSurface">@color/light_text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_background</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
